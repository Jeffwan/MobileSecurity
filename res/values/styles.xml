<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="text_content_style">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">19sp</item>
        <item name="android:paddingLeft">8dip</item>
        <item name="android:layout_marginTop">5dip</item>
    </style>

    <style name="button_next_style">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:layout_alignParentRight">true</item>
        <item name="android:drawableRight">@drawable/next</item> <!-- this is a picture from system? -->
        <item name="android:text">Next</item>
        <item name="android:onClick">next</item>
    </style>

    <style name="button_pre_style" parent="@style/button_next_style">
        <item name="android:layout_alignParentRight">false</item>
        <item name="android:drawableRight">@null</item> <!-- what's this? because of parent,set null -->
        <item name="android:drawableLeft">@drawable/previous</item>
        <item name="android:text">Previous</item>
        <item name="android:onClick">pre</item>
    </style>

    <style name="my_pb_style" parent="@android:style/Widget.ProgressBar">
        <item name="android:indeterminateDrawable">@drawable/progress_medium_white</item>
    </style>

    <style name="my_progreebar_style" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:progressDrawable">@drawable/progress_horizontal</item>
    </style>

</resources>